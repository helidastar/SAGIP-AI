import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Responder Login"
      description="Sign in with your responder or admin account."
    />
  );
}
